<%- include('./partials/header') %>

<div class="container-fluid">
	<div class="row">
		<h3>Cart</h3>
		<hr>
		<h4>items</h4>

		<div class="col-xs-12 col-md-8">
			<% if (order) { %>
			<% order.products.forEach(function(p){ %>
				<% console.log(p) %>
			<div class="row">
				<p> <%= p.product.label + p.quantity %></p>
			</div>
			<% }); %>
			<% } %>
		</div>
	</div>
</div>



<%- include('./partials/footer') %>